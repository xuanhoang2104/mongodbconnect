# 1. Commit changes
git commit -m "feat: add complete MLOps project structure

- Add project structure with components and pipelines
- Add MongoDB database integration
- Add configuration files
- Add testing framework
- Add CI/CD workflows"

# 2. Đổi tên branch
git branch -M main

# 3. Kiểm tra branch
git branch

# 4. Push lên GitHub
git push -u origin main




##############

git add .
git commit -m "mô tả thay đổi"
git push